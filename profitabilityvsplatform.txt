select 
platform,
case
when ROAS >=1  then  'Profitable'
else 'Not profitable'
end as profitability,
COUNT(*) as count 
from Ads_performance
group by platform,
case
when ROAS >=1  then  'Profitable'
else 'Not profitable'
end 
